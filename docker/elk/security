docker cp {directory}-es01-1:/usr/share/elasticsearch/config/certs/ca/ca.crt /tmp/.

docker cp elk-es01-1:/usr/share/elasticsearch/config/certs/ca/ca.crt /tmp/.

curl --cacert /tmp/ca.crt -u elastic:changeme https://localhost:9200